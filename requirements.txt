# Core FEN logic depends only on the standard library.
# The following are needed for rendering, augmentation, and training.

# Rendering
python-chess>=1.11
cairosvg>=2.7
Pillow>=10.0

# Augmentation
numpy>=1.24
opencv-python>=4.8

# Model training
torch>=2.1
